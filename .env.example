# Optional: only needed when the frontend is deployed and the API is on a different origin.
# Leave unset for local dev (Vite proxies /api to localhost:5000).
# Set this in your deployment (Render, Netlify, etc.) so the built app calls the real API.
# VITE_API_URL=https://pool-8w66.onrender.com
